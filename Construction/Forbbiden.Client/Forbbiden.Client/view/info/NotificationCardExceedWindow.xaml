<Window x:Class="Forbbiden.Client.view.info.NotificationCardExceedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Forbbiden.Client.view.info"
        xmlns:language="clr-namespace:Forbbiden.Client.Properties.Langs"
        mc:Ignorable="d"
        Title="NotificationCardExceedWindow" Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    
    
    <Grid Background="#303873">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <!-- Upper bar -->
        <Grid Grid.Row="0" Background="#EFC437">
            <TextBlock x:Name="titletxtBk" Foreground="White"
                       Text="{x:Static language:Resources.max_cards_exceed}"
                       FontFamily="{StaticResource IrishGrover}"
                       FontSize="48"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- Message -->
        <Grid Grid.Row="1" >
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="130">
                <TextBlock x:Name="messagetxtBk" Foreground="White"
                           FontFamily="{StaticResource IrishGrover}"
                           Text="{x:Static language:Resources.max_cards_exceed_message}"
                           FontSize="38"
                           MaxWidth="620"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           TextAlignment="Center"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </ScrollViewer>

        </Grid>

        <!-- Button -->
        <Grid Grid.Row="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="discardButton" Grid.Column="0"
                    Content="{x:Static language:Resources.discard_card}"
                    Width="340" Height="60"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Margin="0,0,0,30"
                    Foreground="White" Background="#43C414"
                    FontFamily="{StaticResource IrishGrover}"
                    FontSize="32"
                    Cursor="Hand"
                    Click="DiscardButton_Click" >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15"
                                BorderThickness="0" >
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="keepButton" Grid.Column="1"
                    Content="{x:Static language:Resources.keep_cards}"
                    Width="340" Height="60"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    Margin="0,0,0,30"
                    Foreground="White" Background="#43C414"
                    FontFamily="{StaticResource IrishGrover}"
                    FontSize="32"
                    Cursor="Hand"
                    Click="KeepButton_Click" >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15"
                                BorderThickness="0" >
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

    </Grid>
</Window>
