using System;

namespace Forbbiden.Client
{
    public class LoginOriginator
    {
        private string _username { get; set; }
        private string _email { get; set; }

        public LoginOriginator(string username, string email)
        {
            _username = username;
        }

        public LoginOriginator(string username, string email)
        {
            _username = username;
            _email = email;
        }

        public SaveSnapshot()
        {
        }

        public RestoreSnapshot()
        {
        }
    }
}