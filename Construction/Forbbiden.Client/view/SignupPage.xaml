<Page x:Class="Forbbiden.Client.SignupPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Forbbiden.Client"
        xmlns:p ="clr-namespace:Forbbiden.Client.Properties.Langs"
        mc:Ignorable="d"
        Title="SignupPage" >

    <Page.Background>
        <ImageBrush ImageSource="/Images/FEI MainPage3.png" Stretch="Fill"/>
    </Page.Background>

    <Viewbox Stretch="Uniform">
        <Grid Width="1920" Height="1080">
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Background="#0F1945" LastChildFill="True" >
                <Button x:Name="backButton"
                Content="&lt;"
                Width="100" Height="100"
                FontSize="40"
                FontFamily="pack://application:,,,/Fonts/#Irish Grover"
                BorderThickness="5" BorderBrush="#FFB7B7B7"
                Background="White"
                Foreground="Black"
                Margin="20,0,0,0"
                Cursor="Hand"
                DockPanel.Dock="Left"
                Click="BackButton_Click"/>

                <!-- Title -->
                <TextBlock Text="{x:Static p:Resources.signup_title}"
                   FontSize="60"
                   Foreground="White"
                   FontFamily="pack://application:,,,/Fonts/#Irish Grover"
                   FontWeight="Bold"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>
            </DockPanel>

            <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- User -->
                <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,5,0,0">
                    <TextBlock Text="{x:Static p:Resources.username}" HorizontalAlignment="Left" Foreground="White" FontSize="20" FontFamily="pack://application:,,,/Fonts/#Irish Grover" />
                    <TextBox x:Name="txtBUsername" Height="35" Width="280" Margin="0,5" FontSize="20" />
                </StackPanel>

                <!-- Email -->
                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,5,0,0">
                    <TextBlock Text="{x:Static p:Resources.email}" HorizontalAlignment="Left" Foreground="White" FontSize="20" FontFamily="pack://application:,,,/Fonts/#Irish Grover" />
                    <TextBox x:Name="txtBEmail" Height="35" Width="280" Margin="0,5" FontSize="20" />
                </StackPanel>

                <!-- Password -->
                <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,5,0,0">
                    <TextBlock Text="{x:Static p:Resources.password}" HorizontalAlignment="Left" Foreground="White" FontSize="20" FontFamily="pack://application:,,,/Fonts/#Irish Grover" />
                    <TextBox x:Name="txtBPassword" Height="35" Width="280" Margin="0,5" FontSize="20" />
                </StackPanel>

                <!-- Register Button -->
                <StackPanel Grid.Row="4" Orientation="Vertical"
                HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button x:Name="signupButton" Content="{x:Static p:Resources.signup_button}" 
                        Height="35" Width="150" Background="#39B349" 
                        Foreground="White" FontSize="18" FontFamily="pack://application:,,,/Fonts/#Irish Grover"
                        Click="SignupButton_Click" Margin="0,0,0,0"/>
                    <Label Content="{x:Static p:Resources.login_title}" Margin="0,5,0,0" 
                        HorizontalAlignment="Center" VerticalAlignment="Center" 
                        MouseDown="Login_MouseDown" Cursor="Hand" FontFamily="pack://application:,,,/Fonts/#Irish Grover"
                        Foreground="White" FontSize="14" />
                </StackPanel>

            </Grid>
        </Grid>
    </Viewbox>
</Page>